rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only authenticated server-side Admin SDK code may access application data.
    match /{document=**} { allow read, write: if false; }
  }
}
